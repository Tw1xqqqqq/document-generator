<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Сервис конвертации docx -> pdf
    |--------------------------------------------------------------------------
    | Адрес Gotenberg внутри сети Docker. В docker-compose.yml это имя
    | сервиса, поэтому по умолчанию http://gotenberg:3000.
    */

    'gotenberg_url' => env('GOTENBERG_URL', 'http://gotenberg:3000'),

    'gotenberg_timeout' => (int) env('GOTENBERG_TIMEOUT', 120),

    /*
    |--------------------------------------------------------------------------
    | Формат дат в документах
    |--------------------------------------------------------------------------
    | Поля типа «дата» приходят с фронта в виде 2026-09-17,
    | а в печатной форме привычнее 17.09.2026.
    */

    'date_format' => env('DOCUMENT_DATE_FORMAT', 'd.m.Y'),

];
