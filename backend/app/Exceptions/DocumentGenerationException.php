<?php

namespace App\Exceptions;

use RuntimeException;

/**
 * Ошибка, о которой пользователю имеет смысл рассказать:
 * у шаблона нет файла, недоступен конвертер и тому подобное.
 *
 * Отдельный класс нужен, чтобы отличать такие случаи от внутренних сбоев.
 * В боевом режиме тексты обычных исключений наружу не отдаются, иначе
 * в ответе окажутся пути к файлам и устройство приложения.
 */
class DocumentGenerationException extends RuntimeException
{
}
